# Core requirements
streamlit>=1.25.0
pandas>=1.3.0
xlsxwriter>=3.0.0

# Optional - depending on CSV encoding/reading edge cases
numpy>=1.21.0

# Ensure compatibility with newer Python versions (e.g., 3.10+)
typing-extensions>=4.0.0

# For environments where wheels may be needed
wheel
